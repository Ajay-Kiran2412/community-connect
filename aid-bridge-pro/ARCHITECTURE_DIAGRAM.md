# 📊 Push Notifications Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        COMMUNITY CONNECT APP                            │
│                      Push Notification System                           │
└─────────────────────────────────────────────────────────────────────────┘

                            ┌──────────────────┐
                            │   User A Phone   │
                            │   (Browser App)  │
                            └────────┬─────────┘
                                     │
                    ┌────────────────┼────────────────┐
                    │                │                │
                    ▼                ▼                ▼
            ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
            │  User B      │ │  User C      │ │  User D      │
            │  (Web App)   │ │  (Mobile)    │ │  (Tablet)    │
            └──────┬───────┘ └──────┬───────┘ └──────┬───────┘
                   │                │                │
                   │      Device Tokens             │
                   │   (FCM Tokens Stored)          │
                   │                │                │
                   └────────────────┼────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
                    ▼                               ▼
        ┌──────────────────────┐      ┌──────────────────────┐
        │   Frontend Registers │      │  Backend Stores in   │
        │  Device Token with   │      │  MongoDB User Model  │
        │       Backend        │      │  (deviceTokens: [])  │
        └──────────┬───────────┘      └──────────┬───────────┘
                   │                               │
                   │         Save Token            │
                   │  POST /api/auth/device-token  │
                   │◄──────────────────────────────┤
                   │                               │
                   └───────────────────┬───────────┘
                                       │
                                       ▼
                        ┌──────────────────────────────┐
                        │   MongoDB - User Document    │
                        │                              │
                        │  {                           │
                        │    _id: "user123",           │
                        │    name: "John",             │
                        │    deviceTokens: [           │
                        │      "token_abc...",         │
                        │      "token_xyz..."          │
                        │    ]                         │
                        │  }                           │
                        └──────────┬───────────────────┘
                                   │
                    ┌──────────────┴──────────────┐
                    │                             │
                    ▼                             ▼
        ┌──────────────────────┐   ┌──────────────────────┐
        │  User creates a post │   │ Backend receives     │
        │  in the app          │   │ createPost request   │
        └──────────┬───────────┘   └──────────┬───────────┘
                   │                           │
                   │         POST              │
                   │  /api/posts/create        │
                   │──────────────────────────►│
                   │                           │
                   │                           ▼
                   │        ┌────────────────────────────┐
                   │        │  Post saved to MongoDB     │
                   │        └────────────┬───────────────┘
                   │                     │
                   │                     ▼
                   │        ┌────────────────────────────┐
                   │        │ Notification Service is   │
                   │        │ triggered asynchronously  │
                   │        └────────────┬───────────────┘
                   │                     │
                   │                     ▼
                   │        ┌────────────────────────────┐
                   │        │ Get all user device tokens│
                   │        │ (except creator's token)  │
                   │        └────────────┬───────────────┘
                   │                     │
                   │                     ▼
                   │        ┌────────────────────────────┐
                   │        │  Firebase Cloud Messaging │
                   │        │  (FCM) - admin.messaging()│
                   │        └────────────┬───────────────┘
                   │                     │
         ┌─────────┴──────────────────────┼────────┬──────────┐
         │                                │        │          │
         ▼                                ▼        ▼          ▼
   ┌───────────┐              ┌────────────────────────────────┐
   │  Browser  │              │  Firebase Infrastructure       │
   │ Push Svc  │              │  (Google's Servers)            │
   └─────┬─────┘              │  • Validates tokens            │
         │                     │  • Routes notifications        │
         │                     │  • Handles delivery            │
         │                     └────────────┬───────────────────┘
         │                                  │
         │      Notification Delivery       │
         │◄─────────────────────────────────┤
         │                                  │
         ▼          ▼          ▼            ▼
    ┌─────────┐┌────────┐ ┌────────┐ ┌──────────┐
    │Firebase ││Windows │ │ Apple  │ │ Android  │
    │ SDK Web ││Chrome  │ │Safari  │ │ Firebase │
    │Messaging││Push    │ │Push    │ │ Services │
    └────┬────┘└───┬────┘ └───┬────┘ └────┬─────┘
         │          │          │           │
         ▼          ▼          ▼           ▼
    ┌──────────────────────────────────────────────┐
    │           User Notifications                 │
    │  "New Blood Post - John needs AB+ donors"   │
    │          [NOTIFICATION POP-UP]              │
    │  ┌─────────┐  ┌──────────────┐ ┌────────┐  │
    │  │  Click  │  │   Dismiss    │ │ Details│  │
    │  └────┬────┘  └──────────────┘ └────────┘  │
    │       │                                      │
    │       ▼                                      │
    │  ┌──────────────────────────────┐           │
    │  │ Opens app with post details │           │
    │  └──────────────────────────────┘           │
    └──────────────────────────────────────────────┘
```

## Data Flow Diagram

```
┌─────────────────┐
│  User Creates   │
│     Post        │
└────────┬────────┘
         │
         │ POST /api/posts
         ▼
┌─────────────────────────────────────┐
│  Backend: postController.js         │
│  - Validate post data               │
│  - Save to MongoDB                  │
│  - Trigger notifications            │
└────────┬────────────────────────────┘
         │
         │ Async (doesn't wait)
         ▼
┌─────────────────────────────────────┐
│  notificationService.js             │
│  - sendNewPostNotification()        │
│  - Get all user device tokens       │
│  - Build notification payload       │
└────────┬────────────────────────────┘
         │
         │ Batch send
         ▼
┌─────────────────────────────────────┐
│  Firebase Admin SDK                 │
│  - admin.messaging().sendMulticast()│
│  - Send to 500 tokens per batch     │
└────────┬────────────────────────────┘
         │
         │ Deliver
         ▼
┌─────────────────────────────────────┐
│  User Device / Browser              │
│  - Receive notification             │
│  - Show system notification         │
│  - Store in notification center     │
└────────┬────────────────────────────┘
         │
         │ User clicks
         ▼
┌─────────────────────────────────────┐
│  Service Worker                     │
│  - firebase-messaging-sw.js         │
│  - Handle notification click        │
│  - Open app with post link          │
└─────────────────────────────────────┘
```

## Component Integration

```
Frontend App
│
├── App.tsx
│   └── usePushNotifications()
│       ├── initializeMessaging()
│       ├── requestNotificationPermission()
│       ├── setupMessageListener()
│       └── getDeviceToken()
│
├── Components
│   └── NotificationToggle
│       └── Button to enable/disable
│
├── Services
│   └── firebaseMessaging.ts
│       ├── initializeFirebase()
│       ├── handleNotifications()
│       └── manageDeviceTokens()
│
└── Public
    └── firebase-messaging-sw.js
        ├── Background message handler
        ├── Notification click handler
        └── App launch on notification


Backend App
│
├── server.js
│   └── Express initialization
│
├── src/
│   ├── models/
│   │   └── userModel.js (+ deviceTokens)
│   │
│   ├── services/
│   │   └── notificationService.js
│   │       ├── initializeFirebase()
│   │       ├── sendNotification()
│   │       ├── sendMulticastNotification()
│   │       └── sendNewPostNotification()
│   │
│   ├── controllers/
│   │   ├── authController.js
│   │   │   ├── saveDeviceToken()
│   │   │   └── removeDeviceToken()
│   │   │
│   │   └── postController.js
│   │       └── createPost()
│   │           └── trigger notifications
│   │
│   └── routes/
│       ├── authRoutes.js (+ device token routes)
│       └── postRoutes.js
│
└── .env (Firebase credentials)
```

## Notification Payload Structure

```json
{
  "notification": {
    "title": "New Blood Post",
    "body": "John needs AB+ donors urgently"
  },
  "data": {
    "postId": "507f1f77bcf86cd799439011",
    "category": "blood",
    "type": "new_post",
    "link": "/posts/507f1f77bcf86cd799439011",
    "userId": "507f191e810c19729de860ea"
  },
  "android": {
    "priority": "high",
    "notification": {
      "sound": "default",
      "channelId": "community_connect"
    }
  },
  "apns": {
    "alert": {
      "title": "New Blood Post",
      "body": "John needs AB+ donors urgently"
    },
    "sound": "default"
  },
  "webpush": {
    "fcmOptions": {
      "link": "/posts/507f1f77bcf86cd799439011"
    }
  }
}
```

## Database Schema Changes

```javascript
// User Model
{
  _id: ObjectId,
  name: String,
  email: String,
  password: String,
  role: String,
  verified: Boolean,
  badges: [String],
  
  // NEW FIELDS FOR NOTIFICATIONS
  deviceTokens: [String],  // Firebase FCM tokens
  notificationPreferences: {
    newPosts: Boolean,     // Get notified on new posts
    allCategories: Boolean, // Or subscribe to specific categories
    categories: [String]   // List of subscribed categories
  },
  
  createdAt: Date,
  updatedAt: Date
}
```

## API Endpoints Added

```
┌──────────────────────────────────────┐
│   Device Token Management            │
├──────────────────────────────────────┤
│                                      │
│  POST /api/auth/device-token         │
│  ├── Purpose: Register device        │
│  ├── Auth: Required (JWT)            │
│  ├── Body: { deviceToken: "..." }    │
│  └── Response: { status, tokenCount }│
│                                      │
│  DELETE /api/auth/device-token       │
│  ├── Purpose: Unregister device      │
│  ├── Auth: Required (JWT)            │
│  ├── Body: { deviceToken: "..." }    │
│  └── Response: { status, tokenCount }│
│                                      │
└──────────────────────────────────────┘
```

## Environment Variables

```
Backend (.env)
├── FIREBASE_SERVICE_ACCOUNT (full JSON)
└── FIREBASE_DATABASE_URL

Frontend (.env.local)
├── VITE_FIREBASE_API_KEY
├── VITE_FIREBASE_AUTH_DOMAIN
├── VITE_FIREBASE_PROJECT_ID
├── VITE_FIREBASE_STORAGE_BUCKET
├── VITE_FIREBASE_MESSAGING_SENDER_ID
├── VITE_FIREBASE_APP_ID
└── VITE_FIREBASE_VAPID_KEY
```

---

This diagram shows the complete flow of how push notifications work in your Community Connect app!
